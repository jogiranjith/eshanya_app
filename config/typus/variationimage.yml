# Use the README file as a reference to customize settings.

Variationimage:
  fields:
    default: variation_images, product_id
    form: variation_images, product_id
  relationships: 
  application: Application
